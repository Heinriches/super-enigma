# len(0 is one of the most useful built-in functions.

# len() stands for 'length' and, when used on lists,
# it returns the number of items in the list.

movies = ["Avatar", "Titanic", "Avengers"]
print(len(movies))
