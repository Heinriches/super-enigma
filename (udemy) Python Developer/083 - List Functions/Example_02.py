# The append(0 function adds a new item to the end of a list.

# 'append()' is called using dot-notation, because it's
# specific to lists.

songs = ["Yesterday", "Hello", "Believer"]
songs.append("Imagine")
print(songs)