# The pop() functions removes an element from a list.

# That position indicated by the index is the only argument
# that the pop() function accepts.

items = ["book", "pen", "pencil"]
items.pop(1)
print(items)
print(items[1])