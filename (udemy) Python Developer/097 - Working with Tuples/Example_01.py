# Tuples, like lists, can contain duplicate
# elements.

# You can use the 'count()' function to
# calculate thenumber of occurences of
# an item in a tuple.

scores = (7, 9, 9, 8, 9)
print("# of 7:", scores.count(7))
print("# of 9:", scores.count(9))
print("# of 2:", scores.count(2))