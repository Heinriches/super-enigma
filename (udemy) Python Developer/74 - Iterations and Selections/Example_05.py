# Code a nested loop to create a list of repetitive
# tasks for each day:

days = ['Monday', 'Tuesday', 'Wednesday']
tasks = ['Gym', 'Homework', 'Pool']

for day in days:
    for task in tasks:
        print(day, task)